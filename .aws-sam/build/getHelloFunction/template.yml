# This is the SAM template that represents the architecture of your serverless application
# https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/serverless-sam-template-basics.html

# The AWSTemplateFormatVersion identifies the capabilities of the template
# https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/format-version-structure.html
AWSTemplateFormatVersion: 2010-09-09
Description: >-
  matic-rebalancer

# Transform section specifies one or more macros that AWS CloudFormation uses to process your template
# https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/transform-section-structure.html
Transform:
  - AWS::Serverless-2016-10-31

# Resources declares the AWS resources that you want to include in the stack
# https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/resources-section-structure.html
Resources:
  # Each Lambda function is defined by properties:
  # https://github.com/awslabs/serverless-application-model/blob/master/versions/2016-10-31.md#awsserverlessfunction

  getHelloFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: built/handlers/get-hello.getHelloHandler
      Runtime: nodejs12.x
      MemorySize: 128
      Timeout: 100
      Description: Prints a greeting message.
      Events:
        Api:
          Type: Api
          Properties:
            Path: /hello
            Method: GET

  postFaucetFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: built/handlers/post-faucet.postFaucetHandler
      Runtime: nodejs12.x
      MemorySize: 128
      Timeout: 100
      Description: Request tokens from hosted Connext client.
      Policies:
        # Give Create/Read/Update/Delete Permissions to the SampleTable
        - DynamoDBCrudPolicy:
            TableName: !Ref Users
      Environment:
        Variables:
          # Make table name accessible as environment variable from function code during execution
          TWITTER_CONSUMER_API_KEY: hddjRRpzzVplLPiZj2l4AsmRj
          TWITTER_CONSUMER_API_SECRET: KrHHZ2hUNKPVj8mkFE28Q78QG4nXHhPJEDuhauK9f9tqm6JMPb
          CONNEXT_CLIENT_URL: "http://18.223.112.148:5040"
          TOKEN_ADDRESS: "0x50C94BeCAd95bEe21aF226dc799365Ee6B134459"
          DYNAMO_TABLE: !Ref Users
          NODE_ENV: production
      Events:
        Api:
          Type: Api
          Properties:
            Path: /faucet
            Method: POST

Outputs:
  WebEndpoint:
    Description: "API Gateway endpoint URL for Prod stage"
    Value: !Sub "https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"
